{% set pdfURL %}
    {{content.field_archivo_pdf}}
{% endset %}
<object data="{{ pdfURL|striptags }}" type="application/pdf" width="100%" height="750px">
    <embed src="{{ pdfURL|striptags }}" type="application/pdf">
        <p>Tu navegador no soporta el formato PDF, podés bajarlo y abrirlo en tu PDF Reader: <a href="{{ pdfURL|striptags }}">Descargar en PDF</a>.</p>
    </embed>
</object>





